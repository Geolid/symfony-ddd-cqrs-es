{% extends 'layout.html.twig' %}
{% import '@ui/_macros.html.twig' as ui %}

{% block title %}{{ 'shipments.index.title'|trans }}{% endblock %}
{% block heading %}{{ 'shipments.index.title'|trans }}{% endblock %}

{% block content %}
    <table>
        <thead>
        <tr>
            <th>{{ 'shipments.index.table.id'|trans }}</th>
            <th>{{ 'shipments.index.table.order'|trans }}</th>
            <th>{{ 'shipments.index.table.customer'|trans }}</th>
            <th>{{ 'shipments.index.table.total'|trans }}</th>
            <th>{{ 'shipments.index.table.status'|trans }}</th>
            <th>{{ 'shipments.index.table.created_at'|trans }}</th>
            <th>{{ 'shipments.index.table.dispatched_at'|trans }}</th>
            <th>{{ 'shipments.index.table.delivered_at'|trans }}</th>
            <th>{{ 'shipments.index.table.order_cancelled_at'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for shipment in shipments.items %}
            <tr>
                <td>{{ shipment.id }}</td>
                <td>{{ shipment.orderId }}</td>
                <td>{{ shipment.customerId ?? '—' }}</td>
                <td>{{ shipment.orderTotalInCents is not null ? (shipment.orderTotalInCents / 100)|number_format(2) : '—' }}</td>
                <td><span class="status">{{ ('shipments.status.' ~ shipment.status)|trans }}</span></td>
                <td>{{ shipment.createdAt|date('Y-m-d H:i') }}</td>
                <td>{{ shipment.dispatchedAt ? shipment.dispatchedAt|date('Y-m-d H:i') : '—' }}</td>
                <td>{{ shipment.deliveredAt ? shipment.deliveredAt|date('Y-m-d H:i') : '—' }}</td>
                <td>{{ shipment.orderCancelledAt ? shipment.orderCancelledAt|date('Y-m-d H:i') : '—' }}</td>
            </tr>
        {% else %}
            <tr><td colspan="9">{{ 'shipments.index.empty'|trans }}</td></tr>
        {% endfor %}
        </tbody>
    </table>
    {{ ui.pagination(shipments.pagination, 'shipments_index', {}) }}
{% endblock %}
