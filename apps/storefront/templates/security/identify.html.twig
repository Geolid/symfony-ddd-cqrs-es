{% extends 'layout.html.twig' %}

{% block importmap %}{{ importmap(['shared', 'auth']) }}{% endblock %}

{% block title %}{{ 'title'|trans({}, 'signin') }}{% endblock %}

{% block content %}
    <article>
        <h1>{{ 'title'|trans({}, 'signin') }}</h1>

        {{ form_start(form) }}
            {{ form_row(form.email) }}
            <button type="submit">{{ 'button_submit'|trans({}, 'signin') }}</button>
        {{ form_end(form) }}
    </article>
{% endblock %}
