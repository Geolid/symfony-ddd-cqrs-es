{% extends 'layout.html.twig' %}

{% block title %}Vérification à deux facteurs{% endblock %}

{% block body %}
    <h1>Vérification à deux facteurs</h1>

    {% if authenticationError %}
        <p data-testid="totp-error">{{ authenticationError|trans(authenticationErrorData, 'security') }}</p>
    {% endif %}

    <form method="post" action="{{ checkPathUrl ?? path(checkPathRoute) }}" data-testid="totp-form">
        <label for="totp-code">Code à 6 chiffres</label>
        <input type="text" id="totp-code" name="{{ authCodeParameterName }}" autocomplete="one-time-code" required autofocus>

        {% if isCsrfProtectionEnabled %}
            <input type="hidden" name="{{ csrfParameterName }}" value="{{ csrf_token(csrfTokenId) }}">
        {% endif %}

        <button type="submit" data-testid="totp-submit">Valider</button>
    </form>

    <p><a href="{{ logoutPath }}" data-testid="totp-logout">Se déconnecter</a></p>
{% endblock %}
