{% extends 'layout.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(form, {attr: {'data-testid': 'register-form'}}) }}
        {{ form_widget(form) }}
        <button type="submit" data-testid="register-submit">S'inscrire</button>
    {{ form_end(form) }}
{% endblock %}
