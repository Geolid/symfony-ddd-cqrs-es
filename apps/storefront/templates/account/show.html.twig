{% extends 'layout.html.twig' %}

{% block title %}{{ 'title'|trans({}, 'account') }}{% endblock %}

{% block content %}
    <h1>{{ 'title'|trans({}, 'account') }}</h1>

    <p><a href="{{ path('storefront_account_security') }}">{{ 'link_security'|trans({}, 'account') }}</a></p>
{% endblock %}
